# Development environment variables for cumments project

# Database connection string used by sqlx-cli for migrations during development
# This is only used for running sqlx-cli commands like `sqlx database create`
# The application itself reads the database URL from config.toml or CUMMENTS_DATABASE__URL
DATABASE_URL=sqlite://data/cumments.db
