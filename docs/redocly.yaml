extends:
  - recommended
rules:
  info-license: error
  no-server-example.com: error
  no-ambiguous-paths: error
  operation-4xx-response: error
  no-unused-components: error
  # Matrix room IDs contain ':' and cannot collide with literal path segments.
